name: super_pagination
description: >
  Powerful Flutter pagination library with built-in BLoC state management,
  6+ view types (ListView, GridView, PageView, StaggeredGrid, ReorderableListView,
  Column, Row, Custom), advanced error handling (6 pre-built error styles),
  smart preloading, real-time stream support, automatic retry with exponential
  backoff, SuperSearchBox with auto-positioning overlay dropdown, keyboard navigation, and production-ready UI components..

# package version
version: 4.1.6

# package homepage, repository and issue tracker urls
homepage: https://github.com/GeniusSystems24/super_pagination
repository: https://github.com/GeniusSystems24/super_pagination
issue_tracker: https://github.com/GeniusSystems24/super_pagination/issues

# Ignore false positive secret leaks (Firebase client-side API keys are safe to expose)
false_secrets:
  - /docs/**
  - /example/lib/firebase_options.dart
  - /example/lib/app/bootstrap/firebase_options.dart
  - /example/android/app/google-services.json
  - /example/ios/Runner/GoogleService-Info.plist


environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_staggered_grid_view: ^0.7.0
  logger: ^2.6.2
  provider: ^6.1.5+1
  scrollview_observer: ^1.26.2
  super_core: ">=2.1.0 <3.0.0"

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.1
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
# flutter: null

# To add assets to your package, add an assets section, like this:
# assets:
#   - images/a_dot_burr.jpeg
#   - images/a_dot_ham.jpeg
#
# For details regarding assets in packages, see
# https://flutter.dev/to/asset-from-package
#
# An image asset can refer to one or more resolution-specific "variants", see
# https://flutter.dev/to/resolution-aware-images
# To add custom fonts to your package, add a fonts section here,
# in this "flutter" section. Each entry in this list should have a
# "family" key with the font family name, and a "fonts" key with a
# list giving the asset and other descriptors for the font. For
# example:
# fonts:
#   - family: Schyler
#     fonts:
#       - asset: fonts/Schyler-Regular.ttf
#       - asset: fonts/Schyler-Italic.ttf
#         style: italic
#   - family: Trajan Pro
#     fonts:
#       - asset: fonts/TrajanPro.ttf
#       - asset: fonts/TrajanPro_Bold.ttf
#         weight: 700
#
# For details regarding fonts in packages, see
# https://flutter.dev/to/font-from-package
